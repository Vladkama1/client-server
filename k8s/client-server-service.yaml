apiVersion: v1
kind: Service
metadata:
  name: loanrequest-server
  namespace: dev
spec:
  type: NodePort
  selector:
    app: loanrequest-server
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 