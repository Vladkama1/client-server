apiVersion: v1
kind: Service
metadata:
  name: client-server
  namespace: dev
spec:
  type: NodePort
  selector:
    app: client-server
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30002